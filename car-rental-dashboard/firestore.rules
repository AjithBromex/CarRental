rules_version = '2';

// Cloud Firestore Rules for Fleetline Car Rental Manager
// Allows full access to vehicles, rentals, and analytics
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
